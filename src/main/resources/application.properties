# Media Ratings Platform - Configuration
# Local secret values should be provided via environment variables or .env.
# Azure DevOps should provide these values via pipeline variables or Azure Key Vault.

# Database Configuration
db.url=jdbc:postgresql://localhost:5432/mrp_db
db.username=mrp_user
db.password=

db.driver=org.postgresql.Driver

# Server Configuration
server.port=8080
server.host=localhost